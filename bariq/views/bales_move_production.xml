<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit the existing stock move form view -->
        <record id="view_stock_move_form_inherit" model="ir.ui.view">
            <field name="name">stock.move.form.inherit</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_move_form"/> <!-- Update to correct XML ID -->
            <field name="arch" type="xml">
                <xpath expr="//field[@name='product_id']" position="after">
                    <!-- Add the production_bales_number field here -->
                    <field name="production_bales_number"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
